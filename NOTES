Priorities:
 - get image signing working
 - remove junk from manager output and add return values to asterisk_exec()
 - get indications.conf working 
 - strip excess prompts!
 - update status.php
 - remove excess icons
 - css irregularities
 - figure out config xml single var irregularities (empty defines showing up)
 - better field checks
 - cdr logs not spacing date correctly, need to be prettied with hr, min, sec.
 - console screen should fill window
 - url given in console menu should match port given in gui (m0n0wall bug)
 - fix logo width
 - vcheck.php on server needs help and so does the firmware query page
 - update all (C) tags

 - presentation
 - website
 - user documentation

 - cleanups!

First Public Beta

- get "advanced mode" working
  - note: whole conf files encoded in base64 and stored in xml conf
  - note: editor needs to be merged with exec.php
 - provider to provider access with pin code
 - access able to be given to existing phones/providers while creating a new provider/phone with a expandable dropdown (a la gmail file upload)
 - QoS options, such as jitterbuffer and packet types supported
 - dialplan is a movable grid

-----------------------------------------------------------------------

Config Snippets:

<dialplan>
	<group>
		<name>Michael I</name>
		<type>sequential</type>
		<groupextension>SIP-PHONE-23lkjfd3209jf20w9djf</groupextension>
		<groupextension>SIP-PHONE-4kjf38jjlk3jw09dj2ks</groupextension>
	</group>
	<group>
		<name>Find Me</name>
		<type>parallel</type>
		<groupextension>SIP-PHONE-23lkjfd3209jf20w9djf</groupextension>
		<groupextension>SIP-PHONE-4kjf38jjlk3jw09dj2ks</groupextension>
	</group>
	
	<quickdial>
		...
	</quickdial>
	
	<customextension>
		...
	</customextension>
<dialplan>
	